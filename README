# TODO

* I don't think `icon.active` setting should occur in the RatingWidget.
* Another approach for setting icons visible and active.
* Write a few tests.
* Icon should send a signal rather than call a method on the parent.

